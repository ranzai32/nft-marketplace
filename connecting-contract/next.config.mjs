/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['aquamarine-known-mastodon-402.mypinata.cloud', 'gateway.pinata.cloud'],
  },
};

export default nextConfig;
